# Example only. Do not commit real values.
# Create the real secret with:
#   kubectl -n jira-asana-sync create secret generic jira-asana-sync-secrets \
#     --from-literal=JIRA_BASE_URL='https://camunda.atlassian.net' \
#     --from-literal=JIRA_EMAIL='aaron.hubbart@camunda.com' \
#     --from-literal=JIRA_API_TOKEN='xxx' \
#     --from-literal=ASANA_PAT='2/xxx'

apiVersion: v1
kind: Secret
metadata:
  name: jira-asana-sync-secrets
  namespace: jira-asana-sync
type: Opaque
stringData:
  JIRA_BASE_URL: "https://camunda.atlassian.net"
  JIRA_EMAIL: "aaron.hubbart@camunda.com"
  JIRA_API_TOKEN: "REPLACE_ME"
  ASANA_PAT: "REPLACE_ME"
